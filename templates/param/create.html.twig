{% extends 'base.html.twig' %}

{% block title %}Parameters{% endblock %}

{% block body %}

    <div class="container">
        <div>
            <a href="{{ path('params') }}" class="change-type">< Powrót</a>
        </div>

        <h1>Dodaj parametr</h1>

        {{ form_start(form) }}
        <div class="form-error">
            {{ form_errors(form) }}
        </div>

        {{ form_row(form.name) }}

        <div class="col" id="some-custom-id">
            {{ form_row(form.value) }}
        </div>

        <div class="col" id="some-custom-id">
            {{ form_row(form.exam) }}
        </div>

        <div class="col" id="some-custom-id">
            {{ form_row(form.submit) }}
        </div>

        {{ form_end(form) }}
    </div>

{% endblock %}
